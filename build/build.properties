#locations
dir.artifacts=/Users/lmajano/exports/artifacts
dir.exports=${dir.artifacts}/${cpu.slug}/${cpu.version}
dir.src=${basedir}/src
dir.build=${basedir}/build-cpu

#remote repos
box.repo.url=http://integration.staging.ortussolutions.com/artifacts

#bleeding edge info
be.exports=/Users/lmajano/exports/artifacts/${cpu.slug}