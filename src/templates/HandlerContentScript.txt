/**
* |Description|
*/
component{
	
	// OPTIONAL HANDLER PROPERTIES
	this.prehandler_only 	= "";
	this.prehandler_except 	= "";
	this.posthandler_only 	= "";
	this.posthandler_except = "";
	// REST Allowed HTTP Methods Ex: this.allowedMethods = {delete='POST,DELETE',index='GET'}
	this.allowedMethods = {};
	
	/**
	IMPLICIT FUNCTIONS: Uncomment to use
	function preHandler(event,action) output=false{
		var rc = event.getCollection();
	}
	function postHandler(event,action) output=false{
		var rc = event.getCollection();
	}
	function onMissingAction(event,missingAction) output=false{
		var rc = event.getCollection();
	}
	function onError(event,faultAction,exception) output=false{
		var rc = event.getCollection();
	}
	*/
		
|EventActions|
	
}